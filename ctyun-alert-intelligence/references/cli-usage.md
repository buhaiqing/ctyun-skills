# CLI Usage — Alert Intelligence

`cli_applicability: sdk-only`

The CTyun CLI does **not** expose alert intelligence analysis commands. All
operations must use the SDK/REST API.

See `references/api-sdk-usage.md` for SDK usage examples.
